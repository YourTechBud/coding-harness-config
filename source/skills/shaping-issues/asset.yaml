kind: skill
frontmatter:
  description: Shape product epics and stories, incorporate spike findings, and configure or use repository issue-tracker guidance. Invoke only on explicit user request.
targets:
  opencode: {}
  pi:
    frontmatter:
      disable-model-invocation: true
  claude:
    frontmatter:
      disable-model-invocation: true
  codex:
    policy:
      allow_implicit_invocation: false
